{
  "name": "express-bootstrap",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node app",
    "test": "mocha --reporter spec"
  },
  "dependencies": {
    "express": "^4.2.0"
  },
  "devDependencies": {
    "should": "^3.3.1",
    "supertest": "^0.12.1",
    "mocha": "^1.19.0"
  }
}
